<div class="homepage-container page-header col-md-12">

  <div class="tag-container col-md-12">
    <h1 class="header"><span class="homepage-header">sixtwelve</span><br><small class="header">Easily share prayers with one another.</small></h1>
  </div>

  <div class="instructions-container col-md-12">
    <h2 class="header">Instructions</h2>
      <h5 class="header">Once you have signed in, email <a href="mailto:sixtwelveinfo@gmail.com" id="email">sixtwelveinfo@gmail.com</a> with your <span id="instruction-text">full name</span> and the <span id="instruction-text">group</span> you wish to join.</h5>
      <h5 class="header"><span id="instruction-text">sixtwelve</span> will confirm your information and add you as a member of the requested group.<br>Enjoy!</h5>
  </div>

  <div class="group-index-container col-md-12">

    <% if signed_in? %>

      <div class="col-md-12">
        <h1 class="header">Please select a group</h1>
      </div>
      <div class="col-md-12">
        <table class="table">
          <% @groups.each do |group| %>
            <tr>
              <td><%= group.name %></td>
              <% if @memberships.include?(group.id) %>
                <td><%=link_to 'Enter', group_path(group), class: 'group-link' %></td>
              <% else %>
                <td><%= link_to 'Request to join', group_member_requests_path(group), method: :post %></td>
              <% end %>
            </tr>
          <% end %>
        </table>
      </div>
    <% else %>
      <h2 class="header" id="sign-in-message">Please sign in to join your group.</h2>
      <div class="col-md-12">
        <h1 class="header">Active Groups</h1>
      </div>
      <div class="col-md-12">
        <table class="table">
          <% @groups.each do |group| %>
            <tr>
              <td><%= group.name %></td>
            </tr>
          <% end %>
        </table>
      </div>
    <% end %>
  </div>

</div>
